variables:
  ENVIRONMENT:
    description: "Select the environment. Valid options are: 'DEV'"
    value: "DEV"

  FEATURE:
    description: "Select thes Feature that you want to execute"
    value: "All" 
    options:
      - "All"
      - "UC-FLIGHTS-HOTELS"
      - "UC-FLIGHTS"
      - "UC-STAYS"

  BROWSER:
    description: "Select the browser. Valid options are: 'chrome', 'edge'"
    value: "chrome" 
    options:
      - "chrome"
      - "edge"